// This file exists only because recent Flutter Gradle tooling checks for a
// settings file at the Flutter project root (alongside pubspec.yaml). The
// real, active Gradle project root is still android/ — this file is not
// used to build anything itself.
